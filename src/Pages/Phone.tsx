
// Phone Product Component (Nested Route)
// This is a nested route component under /Product/phone
// It displays content specific to phones when that route is accessed

const Phone = () => {
  // Return JSX markup for the phone product page
  return (
    <div>
      {/* Display phone product information */}
      Phone
    </div>
  );
};

// Export component as default
export default Phone;