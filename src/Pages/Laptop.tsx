
// Laptop Product Component (Nested Route)
// This is a nested route component under /Product/laptop
// It displays content specific to laptops when that route is accessed

const Laptop = () => {
  // Return JSX markup for the laptop product page
  return (
    <div>
      {/* Display laptop product information */}
      Laptop
    </div>
  );
};

// Export component as default
export default Laptop;