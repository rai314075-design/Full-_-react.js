
// Home Page Component
// This is a simple functional component that displays the home page content
// It uses React's const arrow function syntax to define the component

const Home = () => {
  // Return JSX - HTML-like syntax used in React to describe UI
  return (
    <div>
      {/* Main heading for the home page */}
      <h1>Home Page</h1>
      {/* Welcome paragraph displayed to users */}
      <p>Welcome to our home page!</p>
    </div>
  );
};

// Export the Home component as default so it can be imported and used in other files
export default Home;