
// Tablet Product Component (Nested Route)
// This is a nested route component under /Product/tablet
// It displays content specific to tablets when that route is accessed

const Tablet = () => {
  // Return JSX markup for the tablet product page
  return (
    <div>
      {/* Display tablet product information */}
      Tablet
    </div>
  );
};

// Export component as default
export default Tablet;